# BGE Reranker API Requirements
# Based on the existing environment with PyTorch 2.8.0+cu128 and Transformers 4.57.6

# Core dependencies for the API
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Core dependencies for the reranker
FlagEmbedding>=1.2.11

# Existing environment dependencies (already in openclaw)
torch>=2.8.0+cu128
transformers>=4.57.6

# Additional dependencies that might be needed
sentence-transformers>=2.7.0
huggingface-hub>=0.20.2
numpy>=1.24.0
pandas>=2.1.0

# Optional: for development
python-multipart>=0.0.6

# For asyncio and concurrent processing
asyncio